#!/bin/sh
unset IFS
set -euf

usage() {
    printf "Usage: %s <video>\n" "$0"
}

if [ "$#" != 1 ]; then
    usage
    exit 1
fi

ffprobe \
    -v error \
    -select_streams v:0 \
    -show_entries stream=width \
    -of default=nw=1:nk=1 \
    "$1"
